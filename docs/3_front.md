Техническое проектирование My_FileDB - Этап 3: Веб-интерфейс
3.1 Архитектура пользовательского интерфейса
Общая концепция дизайна
Интерфейс построен по принципу "одна страница - все функции" с четким разделением на логические блоки:

text
┌─────────────────────────────────────────────────┐
│                  ШАПКА                          │
│  My_FileDB 🗃️ [Создать] [Загрузить] [Backup]   │
├─────────────────────────────────────────────────┤
│              ПАНЕЛЬ УПРАВЛЕНИЯ                  │
│ [Поиск...] [Отдел:все] [Найти] [Очистить]       │
│              [➕ Добавить] [🔄 Обновить]         │
├─────────────────────────────────────────────────┤
│              ФОРМА СОТРУДНИКА                   │
│  (отображается при добавлении/редактировании)   │
├─────────────────────────────────────────────────┤
│               ТАБЛИЦА ДАННЫХ                    │
│ ID | ФИО | Отдел | Должность | Зарплата | Дата  │
│ 123|Иван | IT    | Разработч | 1500.50  | 2023  │
├─────────────────────────────────────────────────┤
│                 СТАТУС БАР                      │
│ Готов к работе                    ⏳ Загрузка...│
└─────────────────────────────────────────────────┘
Компонентная структура
1. Header (Шапка приложения)

Назначение: Управление жизненным циклом базы данных

Элементы:

Логотип и название системы

Кнопки управления БД: Создать, Загрузить, Backup, Excel

Индикатор состояния БД (загружена/не загружена)

2. Controls (Панель управления)

Назначение: Поиск и фильтрация данных

Элементы:

Текстовый поиск по ФИО

Выпадающий фильтр по отделам

Кнопки управления данными: Добавить, Обновить

3. Form Section (Форма сотрудника)

Назначение: Ввод и редактирование данных сотрудников

Режимы работы: Добавление нового / Редактирование существующего

Валидация: Клиентская проверка обязательных полей и форматов

4. Table Section (Таблица данных)

Назначение: Основное представление данных в табличном формате

Функциональность:

Отображение всех полей сотрудника

Кнопки действий для каждой записи (редактировать/удалить)

Счетчик загруженных записей

5. Footer (Статус бар)

Назначение: Обратная связь о состоянии системы

Элементы:

Текстовые уведомления о результатах операций

Визуальный индикатор загрузки

Сообщения об ошибках

3.2 Взаимодействие с API
Модель данных клиента
javascript
const AppState = {
    currentDatabase: null,      // Текущая загруженная БД
    employees: [],             // Кэш сотрудников в памяти
    editingEmployee: null,     // Редактируемая запись
    currentFilters: {}         // Активные фильтры поиска
};
Карта API вызовов
Действие UI	API Endpoint	Метод	Обработка ответа
Загрузка всех сотрудников	/employees	GET	Обновление таблицы
Добавление сотрудника	/employees	POST	Обновление таблицы + скрытие формы
Редактирование сотрудника	/employees/{id}	PUT	Обновление таблицы + скрытие формы
Удаление сотрудника	/employees/{id}	DELETE	Обновление таблицы
Поиск сотрудников	/employees/search?params	GET	Обновление таблицы
Создание БД	/database/create	POST	Обновление статуса БД
Загрузка БД	/database/load	POST	Загрузка данных + обновление статуса
Создание backup	/database/backup	POST	Уведомление о успехе
Экспорт в Excel	/export/excel	GET	Открытие в новой вкладке
Обработка состояний загрузки
Визуальные индикаторы:

⏳ Анимированный спиннер во время API запросов

💬 Текстовые уведомления о результате операций

🎨 Цветовая кодировка (зеленый - успех, красный - ошибка)

⏱️ Таймаут автоочистки сообщений (3 секунды)

3.3 Пользовательские сценарии
Сценарий 1: Начало работы
text
1. Пользователь открывает приложение
2. Система показывает сообщение "Создайте или загрузите БД"
3. Пользователь нажимает "Создать БД" → вводит путь
4. Система создает БД → показывает "БД загружена"
5. Автоматически загружается пустая таблица
Сценарий 2: Добавление сотрудника
text
1. Пользователь нажимает "➕ Добавить сотрудника"
2. Появляется форма с пустыми полями
3. Пользователь заполняет данные → нажимает "Сохранить"
4. Система валидирует данные → отправляет на сервер
5. При успехе: форма скрывается, таблица обновляется
6. Показывается уведомление "Сотрудник добавлен"
Сценарий 3: Поиск и фильтрация
text
1. Пользователь вводит текст в поле поиска
2. Выбирает отдел из выпадающего списка  
3. Нажимает "🔍 Найти"
4. Система отправляет запрос с параметрами
5. Таблица обновляется только отфильтрованными данными
6. Показывается уведомление "Найдено X сотрудников"
Сценарий 4: Редактирование записи
text
1. Пользователь нажимает "✏️" в строке таблицы
2. Форма открывается с заполненными данными сотрудника
3. Поле ID заблокировано для редактирования
4. Пользователь изменяет нужные поля → "Сохранить"
5. Система отправляет PUT запрос → обновляет таблицу
3.4 Обработка ошибок и валидация
Клиентская валидация форм
Правила валидации:

ID: число > 0, обязательное поле

ФИО: не пустое, максимум 100 символов

Отдел/Должность: не пустые, максимум 50 символов

Зарплата: число ≥ 0, 2 знака после запятой

Дата приема: корректная дата в формате YYYY-MM-DD

Обработка API ошибок
Стратегия восстановления:

Сетевые ошибки: Повторная попытка не выполняется, показывается сообщение

Ошибки валидации: Подсветка проблемных полей в форме

Бизнес-ошибки: Информативное сообщение с рекомендациями

Системные ошибки: Общее сообщение с предложением обновить страницу

Сообщения пользователю
Успешные операции:

"БД создана успешно"

"Сотрудник добавлен"

"Данные обновлены"

"Найдено X записей"

Ошибки:

"Ошибка сети: невозможно подключиться к серверу"

"Сотрудник с таким ID уже существует"

"Неверный формат данных в поле 'Зарплата'"

"БД не загружена. Сначала создайте или загрузите БД"

3.5 Технические характеристики
Производительность
Минимальные требования: Современный браузер с поддержкой ES6+

Оптимизация: Ленивая загрузка данных, кэширование ответов

Отзывчивость: Незаблокирующий UI во время API запросов

Совместимость
Браузеры: Chrome 60+, Firefox 55+, Safari 12+, Edge 79+

Разрешения: Адаптивный дизайн для desktop (1024px+)

Доступность: Базовая семантическая разметка, keyboard navigation

Объем кода
HTML: ~50 строк (структура страницы)

CSS: ~100 строк (базовые стили и layout)

JavaScript: ~200 строк (логика приложения + API клиент)

Данный интерфейс обеспечивает полный доступ ко всем функциям файловой базы данных через простой и интуитивно понятный веб-интерфейс, соответствующий требованиям лабораторной работы.

